export function Footer() {
  return (
    <footer className="border-t border-border">
      <div className="mx-auto flex max-w-7xl flex-col gap-2 px-5 py-8 font-mono text-[11px] tracking-[0.14em] text-muted-foreground uppercase sm:flex-row sm:items-center sm:justify-between lg:px-10">
        <span>Meeting Analysis AI</span>
        <span>Audio extraction · Transcription · Analysis</span>
      </div>
    </footer>
  );
}
